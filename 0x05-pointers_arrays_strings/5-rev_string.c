#include <stdio.h>

/**
 * rev_string - Function that reverses a string
 * @s: Parameter pointer
 */

void rev_string(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		i++;
	}

	for (i -= 1; i >= 0; i--)
	{
	}
	putchar('\n')
}
